---
- name: Remove launcher symlinks for disabled Python virtualenv applications
  ansible.builtin.file:
    path: "/usr/local/bin/{{ item.launcher_name }}"
    state: absent
  loop: "{{ desktop_python_venv_apps }}"
  loop_control:
    label: "{{ item.name }}"
  when:
    - not (item.enabled | default(true) | bool)
    - item.launcher_name is defined

- name: Remove disabled Python virtualenv application directories
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: absent
  loop: "{{ desktop_python_venv_apps }}"
  loop_control:
    label: "{{ item.name }}"
  when:
    - not (item.enabled | default(true) | bool)

- name: Ensure Python virtualenv application directories exist
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    mode: "0755"
  loop: "{{ desktop_python_venv_apps }}"
  loop_control:
    label: "{{ item.name }}"
  when: item.enabled | default(true) | bool

- name: Create Python virtual environments
  ansible.builtin.command:
    cmd: "python3 -m venv {{ item.path }}"
    creates: "{{ item.path }}/bin/activate"
  loop: "{{ desktop_python_venv_apps }}"
  loop_control:
    label: "{{ item.name }}"
  when: item.enabled | default(true) | bool

- name: Install or update Python virtualenv packages
  ansible.builtin.pip:
    name: "{{ item.packages }}"
    state: latest
    virtualenv: "{{ item.path }}"
  loop: "{{ desktop_python_venv_apps }}"
  loop_control:
    label: "{{ item.name }}"
  when: item.enabled | default(true) | bool

- name: Publish Python application launchers
  ansible.builtin.file:
    src: "{{ item.path }}/bin/{{ item.entrypoint | default(item.launcher_name) }}"
    dest: "/usr/local/bin/{{ item.launcher_name }}"
    state: link
    force: true
  loop: "{{ desktop_python_venv_apps }}"
  loop_control:
    label: "{{ item.name }}"
  when:
    - item.enabled | default(true) | bool
    - item.launcher_name is defined
